apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docs-chart.fullname" . }}-test-connection"
  labels:
    {{- include "docs-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docs-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
